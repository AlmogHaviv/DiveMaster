in order to run the app:
1) enter the venv (.\venv\Scripts\activate)
2) cd the dir where the app is (cd app)
3) enter to run the up (uvicorn main:app --reload) 
how to install packages (pip install fastapi)
go to swagger api ( http://127.0.0.1:8000/docs)